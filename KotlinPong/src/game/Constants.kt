package game

import engine.Color

class Constants {
    companion object {
        val WindowWidth: Int = 400
        val WindowHeight: Int = 300
        val BackgroundColor = Color(1f, 0f, 0f)
        val MainColor = Color(0f, 0f, 0f)
    }
}